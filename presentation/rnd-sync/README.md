# R&D sync slides

Marp deck for two agenda items: pull request reviews, and cluster unification.
Speaker notes live in HTML comments, one block per slide.

Render:

```
npx -y @marp-team/marp-cli@latest slides.md -o slides.html --html
```

Present from `slides.html`; speaker notes are available in Marp's presenter view.
